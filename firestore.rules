
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Global orders list for the tracker
    // ONLY the specific admin email can read this
    match /all_orders/{orderId} {
      allow read: if request.auth != null && request.auth.token.email == 'chetanmadhav4@gmail.com';
      allow write: if true; 
    }

    // User profile and sub-collections
    match /users/{userId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.uid == userId;
      
      match /orders/{orderId} {
        allow read: if request.auth != null && (request.auth.uid == userId || request.auth.token.email == 'chetanmadhav4@gmail.com');
        allow write: if request.auth != null && request.auth.uid == userId;
      }
    }

    // Username mapping
    match /usernames/{username} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    // Bot sessions
    match /botSessions/{sessionId} {
      allow read, write: if true;
    }
  }
}
